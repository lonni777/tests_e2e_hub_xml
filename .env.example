# URL системи для тестування
TEST_BASE_URL=https://hubtest.kasta.ua
TEST_LOGIN_URL=
TEST_DASHBOARD_URL=

# Дані для логіну
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Дані для негативних тестів
TEST_NON_EXISTENT_USER_EMAIL=

# XML-фіди (валідний фід для автотестів і trigger-feedload)
TEST_XML_FEED_URL=https://gist.githubusercontent.com/lonni777/1eb5d08a1dfd4ad0fdf8666ab78ab5be/raw
# Фід з двома варіантами (1-й запит=1 товар, 2-й=2 товари). Тест «вимкнення фіда блокує нові завантаження».
# За замовчуванням localhost:9877 (mock serve-all-feeds.js). Для hubtest — публічний URL або тунель.
# TEST_XML_FEED_TWO_VERSIONS_URL=http://localhost:9877/feed.xml
# Trigger-feedload API (тести налаштувань). Токен — як у Postman.
# TEST_TRIGGER_FEEDLOAD_URL=https://hubtest.kasta.ua/api/admin-tools/trigger-feedload
# TEST_TRIGGER_FEEDLOAD_AUTH=Bearer <token>
# URL з протоколом http (тест «збереження фіду з посиланням http»)
# Hub на hubtest.kasta.ua → публічний HTTP URL (floatrates)
# Hub локально в Docker → host.docker.internal (mock на host)
TEST_HTTP_XML_FEED_URL=http://www.floatrates.com/daily/usd.xml
# TEST_HTTP_XML_FEED_URL=http://host.docker.internal:9876/feed.xml
# URL з .xml але вмістом JSON (невалідна структура) — для негативного тесту
TEST_INVALID_XML_FEED_URL=https://www.dropbox.com/scl/fi/o84mvoxjl0ro6iejsh60p/Untitled-1.xml?rlkey=p09wc82oxv8rfl5c4pho4bfin&st=8k4hz546&dl=1
# URL що повертає 404 (змінено валідний gist)
TEST_404_FEED_URL=https://gist.github.com/lonni777/1eb5d08a1dfd4ad0fdf8666ab78ab5be111/raw
# Невалідний URL (ftp, not-a-url) — для тесту формату
TEST_INVALID_URL_FEED=ftp://test.com
# TC-XML-008: XML з некоректною структурою (зламаний XML)
TEST_INVALID_XML_STRUCTURE_URL=https://gist.githubusercontent.com/lonni777/231bc3625b32b6d8ae95374f154a4e30/raw
# TC-XML-007: URL для тесту conn-timeout 1 хв (non-routable — гарантовано таймаут)
TEST_TIMEOUT_FEED_URL=http://192.0.2.1/xml
TEST_SUPPLIER_NAME=Парфюмс
# Існуючий feed_id для тестування Excel мапінгу (опціонально - для оптимізації тесту)
# За замовчуванням використовується R3DV (фід постачальника Парфюмс)
TEST_EXISTING_FEED_ID=R3DV
# 4 feed_id для тесту обмеження "3 активні фіди" (через кому)
TEST_FEED_IDS_FOR_LIMIT=R3DV,R2K3,R3DX,R3DY

# Налаштування бази даних для cleanup тестів (tests-ts і tests-Python)
TEST_DB_HOST=
TEST_DB_PORT=5432
TEST_DB_NAME=
TEST_DB_USER=
TEST_DB_PASSWORD=
# SSL для підключення (якщо сервер вимагає шифрування, pg_hba.conf): 1 або require
TEST_DB_SSL=